# 00. 과제 요구사항 매핑 (과제 8) — 통과 기준 1:1

상태: ☑ 충족

증거 파일은 모두 `docs/evidence/` 안에 있다. PNG 는 `scripts/capture-evidence.mjs`(Chrome + CDP
WebAuthn 가상 인증기)가 자동 촬영하고, 요청·응답 기록은 `scripts/verify-webauthn.mjs` 가 만든다.
실기기 프롬프트(`T08-E03-register-prompt.png`)만 휴대폰에서 직접 촬영했다.

## 제출물·공개 영역

| ID | 요구 | 충족 위치 | 증거 | 상태 |
|---|---|---|---|---|
| T08-C01 | 결과물 URL 필드에 HTTPS URL 한 개 | https://webtest-passkey.vercel.app | `docs/과제_제출_보고서.md`, `T08-E01-public-incognito.png` | ☑ |
| T08-C02 | 소스 URL 필드에 HTTPS URL 한 개 | https://github.com/stulss/webtest-passkey (public) | `docs/과제_제출_보고서.md` | ☑ |
| T08-C03 | 모든 URL 새 시크릿 창에서 로그인·초대·CAPTCHA 없이 열림 | 공개 `/` 는 인증 없음, GitHub public | `T08-E01-public-incognito.png` (세션 없는 컨텍스트) | ☑ |
| T08-C10 | 첫 화면은 공개 소개, 아무것도 등록 안 해도 열림 | `app/page.tsx` (서버 컴포넌트, DB 조회 0) | `T08-E01-public-incognito.png` | ☑ |
| T08-C11 | 1번 소개 페이지에 이어붙임, 1번 공개 내용 그대로 | `app/portfolio-data.ts`(webtest `PortfolioData.js` 이식), `app/portfolio-sections.tsx` | | ☑ |
| T08-C12 | 실제 개인정보 없음 + 지어낸 내용이라는 사실 적힘 | `app/portfolio-sections.tsx` 의 `.notice` 문단, `portfolio-data.ts` 주석 | 공개 페이지 하단 고지 | ☑ |
| T08-C13 | 공개/비공개 영역 화면에서 구분 | `app/private/page.tsx` — 공개 안내 패널 / `<hr class="area-divider">` / "🔒 비공개 영역" 라벨 | `T08-E05-private-items.png`, `T08-C36-두번째자리_다른내용.png` | ☑ |
| T08-C14 | 비공개 영역 항목 3개 이상 | `app/private/items.tsx` — 항목 추가 폼. 검증 스크립트가 자리마다 3건 생성 | `T08-E05-private-items.png`, `T08-C36-두번째자리_다른내용.png` | ☑ |
| T08-C15 | 패스키 없이는 비공개 내용 화면에 안 보임 | `proxy.ts` → `/private` 익명은 `/login` 으로. `requirePageUser()` | `T08-E02-private-redirect.png` | ☑ |
| T08-C16 | 패스키 없이 서버에 직접 요청하면 거절 | `proxy.ts` + `app/api/items/route.ts` `getSessionUser()→unauthorized()` | `T08-C16-미로그인_API_401.png`, `T08-A06-요청응답기록.png` | ☑ |
| T08-C17 | 그 거절이 401 또는 403 | `unauthorized()` → 401 | `T08-C16-미로그인_API_401.png` (브라우저에 401 + 본문), `webauthn-verify-results.json` | ☑ |
| T08-C18 | 로그인 안 한 페이지 소스에 비공개 내용 없음 | 익명은 `/private` SSR 도달 전 리다이렉트. `GET /api/items` 401 본문에 항목 텍스트 없음 | `T08-E02-미로그인_페이지소스_확인.txt` (소스 검색 0건), `T08-C16-미로그인_API_401.png` | ☑ |

## 등록 (challenge · 공개키 · 이름 · 취소 · 저장 위치)

| ID | 요구 | 충족 위치 | 증거 | 상태 |
|---|---|---|---|---|
| T08-C19 | 서버가 등록용 challenge 만들고 확인할 때까지 보관 | `lib/repository/challenge.ts:create` → KV `challenge:{id}` (EX 300), `lib/service/auth.ts:registerStart` | `T08-A05-DB저장내용.png` (challenge 키 + TTL), `T08-A03-저장된_공개키.txt` | ☑ |
| T08-C20 | 등록 요청마다 challenge 값 다름 기록 | `challenge:audit` LIST + 검증 스크립트 5회 상이 단언 | `T08-A06-요청응답기록.png`, `challenge-log.json` | ☑ |
| T08-C21 | 등록 끝나면 서버에 공개키 저장 | `lib/service/auth.ts:registerFinish` → `lib/repository/credential.ts:create` → KV `cred:{id}.publicKey`(COSE, base64url) | `T08-A05-DB저장내용.png` (저장된 레코드), `T08-A03-저장된_공개키.txt` | ☑ |
| T08-C22 | 저장값이 제출문에 있고 공개키·비밀번호 아님 설명 | `docs/인증_구현_설명서.md` ①·④, `docs/01_기획.md` 키 구조표 | `T08-A05-DB저장내용.png` (COSE 표 + 개인키 `d` 없음 + 비밀번호성 키 0개) | ☑ |
| T08-C23 | 개인키 서버 전송 안 됨이 등록 요청 본문 기록으로 확인 | 등록 요청 본문 = `attestationObject`·`clientDataJSON`·`transports` 만 (개인키 없음). `test/helpers/virtual-authenticator.mjs` 가 만드는 응답 구조 = 실제 브라우저와 동일 | `등록_로그인_요청본문.md` (요청 본문 원문, 개인키 필드 0) | ☑ |
| T08-C24 | 등록한 패스키에 사람이 알아볼 수 있는 이름 | `app/login/page.tsx`·`app/private/passkeys.tsx` label 입력 + `lib/domain/rules.ts:suggestLabel` 자동 제안. KV `cred.label` | `T08-E04-passkey-list.png` (이름 2개) | ☑ |
| T08-C25 | 등록 중간 취소 → 화면 안내 + 서버에 아무것도 저장 안 됨 | `app/webauthn-client.ts` `CancelledError` → `app/private/passkeys.tsx`·`login/page.tsx` 안내 문구. verify 미호출 → challenge 는 TTL 만료, `cred:*` 생성 안 됨 | `T08-E03-register-prompt.png` (취소 버튼 있는 프롬프트), 코드 경로 | ☑ |
| T08-C26 | 패스키 저장 위치(구글 비밀번호 관리자/기기/보안 키) 제출문에 | `lib/dto/records.ts:deviceSentence` (deviceType·backedUp·transports 휴리스틱), 화면 표시 + `docs/인증_구현_설명서.md` ⑥-4 | `T08-E03-register-prompt.png`, `T08-E04-passkey-list.png` (저장 위치 줄) | ☑ |

## 로그인 (challenge · 서명 검증 · 재사용 · 세션 · 비밀번호 없음)

| ID | 요구 | 충족 위치 | 증거 | 상태 |
|---|---|---|---|---|
| T08-C27 | 로그인할 때도 매번 새 challenge | `lib/service/auth.ts:authenticateStart` → `challenge.create` | `T08-A06-요청응답기록.png`, `challenge-log.json` | ☑ |
| T08-C28 | 로그인 요청마다 challenge 값 다름 기록 | 검증 스크립트 5회 상이 | `challenge-log.json`, `challenge_distinct` | ☑ |
| T08-C29 | 저장한 공개키로 서명 확인 뒤에만 통과 | `lib/webauthn.ts:checkAuthentication` → `verifyAuthenticationResponse({ credential:{ publicKey: 저장값 } })`, `lib/service/auth.ts:authenticateFinish` | `서명검증_성공_실패.md` | ☑ |
| T08-C30 | 서명 확인 성공/실패 요청 나란히 | 정상 서명 200 / 1바이트 변조 401 | `서명검증_성공_실패.md`, `signature_and_reuse` | ☑ |
| T08-C31 | 이미 쓴 challenge 재사용 요청 + 거절 응답 | `challenge.consume` = `GETDEL` → 두 번째 nil → 400 | `T08-A06-요청응답기록.png` (재사용 400), `이미쓴질문_재사용.md` | ☑ |
| T08-C32 | 로그인 뒤 무엇으로 알아보는지(세션/토큰) 제출문에 | **세션**. `docs/인증_구현_설명서.md` ①, `lib/session.ts` | `docs/인증_구현_설명서.md` ① | ☑ |
| T08-C33 | 로그아웃 뒤 같은 값 재요청 거절 응답 | `destroySession` → KV `DEL session:{hash}`. 이전 쿠키로 재요청 → `getSessionUser` nil → 401 | `미로그인_차단_요청응답.md`, `blocked_and_logout.after_logout_replay=401` | ☑ |
| T08-C34 | 세션·토큰 값 가려짐 | 검증 스크립트 `redact()` — md 에 `to8_session=<redacted>`, `userHandle:<redacted>`. `results.session_values_recorded:false` | `evidence/*.md` | ☑ |
| T08-C35 | 비밀번호 입력칸 없음 | 로그인/등록 화면에 `type="password"` 없음. `test/routes.test.mjs` 가 정적 검사 | `T08-E03b-login-screen.png`, `npm test` 통과 | ☑ |

## 계정(자리) 간 격리

| ID | 요구 | 충족 위치 | 증거 | 상태 |
|---|---|---|---|---|
| T08-C36 | 패스키 등록 자리 2개, 각각 다른 비공개 내용 | 검증 스크립트 `isolation()` — 자리 A·B 각 3건(내용 다름) | `T08-E05-private-items.png`(A) + `T08-C36-두번째자리_다른내용.png`(B) | ☑ |
| T08-C37 | 한쪽 패스키로 다른쪽 자료 읽기 요청 + 거절 | 자리A → `DELETE /api/items/{B_item}` → 404 | `T08-C37-자리간_접근시도.txt`, `계정간_격리_요청응답.md` | ☑ |
| T08-C38 | 반대 방향도 거절 기록 | 자리B → `DELETE /api/items/{A_item}` → 404 | `T08-C37-자리간_접근시도.txt`, `계정간_격리_요청응답.md` | ☑ |
| T08-C39 | 거절 앞뒤로 반대편 건수 같음 | 3 → 3 (양쪽), `counts_unchanged:true` | `webauthn-verify-results.json` | ☑ |
| T08-C40 | 주소/본문에 다른 계정 적어도 내 자료만 | `GET /api/items?user_id=SPACE_B&owner=SPACE_B` → 자리A 항목 3건만. 라우트가 쿼리·본문의 user_id 를 파싱 안 함 | `T08-C37-자리간_접근시도.txt`, `T08-C36-두번째자리_다른내용.png` | ☑ |
| T08-C41 | 위 거절 만드는 소스 위치 제출문에 | `lib/repository/item.ts` (첫 인자 `spaceId`, 소유자 확인 후에만 삭제) + `lib/session.ts:getSessionUser` (쿠키 전용). `docs/인증_구현_설명서.md` ③ | `T08-C37-자리간_접근시도.txt` 마지막 줄 | ☑ |

## 기기 분실 대비 (패스키 2개 · 삭제 · 0개)

| ID | 요구 | 충족 위치 | 증거 | 상태 |
|---|---|---|---|---|
| T08-C42 | 한 계정에 패스키 2개 | `app/private/passkeys.tsx` [다른 패스키 등록] → 인증된 `register/options` 는 `excludeCredentials` 로 기존 기기 제외, 같은 `userHandle` 재사용 | `T08-E04-passkey-list.png` (2행) | ☑ |
| T08-C43 | 패스키 목록 화면, 이름 + 등록 날짜 | `app/private/passkeys.tsx` — label, `createdAt`(Asia/Seoul), 저장 위치 문구, 마지막 사용 | `T08-E04-passkey-list.png` | ☑ |
| T08-C44 | 패스키 하나 지운 뒤 남은 하나로 로그인 | `lib/service/passkey.ts:remove` → 삭제 후 남은 패스키로 `authenticate/verify` 200 | `T08-A01a-패스키_삭제직후.png` → `T08-A01b-남은패스키로_로그인성공.png` | ☑ |
| T08-C45 | 지운 패스키로는 로그인 불가 기록 | 삭제된 credentialId assertion → `cred:{id}` 조회 실패 → 400 | `T08-C45-등록안된패스키_로그인실패.png`, `패스키_삭제_후.md` | ☑ |
| T08-C46 | 패스키 하나도 안 남았을 때 화면+제출문 | 정책 = **마지막 패스키 삭제 거절(409)**. 자리 전체 삭제는 danger-zone `DELETE /api/space`. `app/private/passkeys.tsx` 문구 + `docs/인증_구현_설명서.md` ⑥-1 | `T08-A01c-마지막패스키_삭제거절.png` (409 안내) | ☑ |

## 설명서·확인 방법·AI 3줄

| ID | 요구 | 충족 위치 | 상태 |
|---|---|---|---|
| T08-C47 | 인증 구현 설명서 6항목 각각 나뉘어 | `docs/인증_구현_설명서.md` ①~⑥ | ☑ |
| T08-C48 | ①에 직접구현/라이브러리/인증서비스 + 이름 | ① "라이브러리 `@simplewebauthn/server`·`/browser` v13, 세션은 직접 구현" | ☑ |
| T08-C49 | ③에 등록·로그인·로그아웃·비공개조회 4흐름 소스 경로 | ③ 표 4행 + 파일 체인 | ☑ |
| T08-C50 | ④에 확인 4가지 각각 성공+거절 나란히 | ④ 표 4행 + `evidence/*.md` | ☑ |
| T08-C51 | ⑥에 아직 못 막은 것 최소 하나 구체적 | ⑥ 6개 (복구 수단 없음 / 레이트리밋 없음 / UV preferred / attestation none / CSRF 토큰 없음 / 인메모리 폴백) | ☑ |
| T08-C52 | 짧은 확인 4항목 나뉘어 | `docs/검증안내서.md` "짧은 확인 4줄" 표 | ☑ |
| T08-C53 | AI 맡긴 일 / 내 판단 / AI 안 따른 일 나뉘어 | `docs/AI_3줄.md` ①②③ | ☑ |
