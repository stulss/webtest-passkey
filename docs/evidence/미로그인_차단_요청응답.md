# 로그인 없이 비공개 자료 요청 + 로그아웃 뒤 재사용 (C16 / C17 / C18 / C34)

```http
GET /api/items   (쿠키 없음)              -> 401 {"error":"로그인이 필요합니다."}
GET /private     (쿠키 없음)              -> 307 location: /login
GET /api/items   (로그인 상태)            -> 200, 3건
POST /api/auth/logout                     -> 200
GET /api/items   (로그아웃 뒤 같은 세션 쿠키 재사용) -> 401 {"error":"로그인이 필요합니다."}
```

> 세션 값은 <redacted> 로 가렸다.
