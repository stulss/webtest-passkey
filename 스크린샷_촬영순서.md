# 증거 스크린샷 — 남은 것은 1장

**8장은 자동으로 찍혔습니다.** `scripts/capture-evidence.mjs` 가 Chrome 을 CDP 로 몰고
DevTools 의 WebAuthn 가상 인증기를 켜서 등록 → 항목 3건 → 두 번째 패스키 → 삭제 →
남은 패스키로 로그인 → 마지막 삭제 거절까지 실제 화면을 `docs/evidence/*.png` 로 저장합니다.

```bash
cd C:/Users/stuls/Desktop/Agent/to8
APP_URL=https://webtest-passkey.vercel.app node scripts/capture-evidence.mjs
```
(촬영용으로 만든 비공개 자리는 끝에 스스로 지웁니다.)

---

## 사람이 직접 찍어야 하는 1장 — `T08-E03-register-prompt.png`

가상 인증기는 OS 프롬프트를 띄우지 않습니다. **실기기 패스키 생성 화면**만 폰으로 찍어 주세요.

1. 휴대폰에서 `webtest-passkey.vercel.app/login` 열기
2. 아래쪽 "새로 만들 패스키 이름" 에 예: `내 폰` 입력
3. **[패스키로 비공개 자리 만들기]** 누르기
4. **구글 비밀번호 관리자 / iCloud 키체인 프롬프트가 뜬 순간** 스크린샷
5. 그 파일을 `docs/evidence/T08-E03-register-prompt.png` 로 저장

→ C25(등록 흐름), C26(패스키를 어디에 저장했는지)

> 이 PC 에는 Windows Hello(지문·얼굴·PIN)가 없어서 Windows 가 USB 보안 키만 제시합니다.
> 그래서 이 한 장은 폰에서 찍습니다. (PC 로 하고 싶으면 Windows 설정 → 계정 → 로그인 옵션에서
> PIN 을 먼저 만들면 됩니다.)

---

## 찍고 나서

```bash
cd C:/Users/stuls/Desktop/Agent/to8
ls docs/evidence/*.png
git add docs/evidence && git commit -m "실기기 등록 프롬프트 스크린샷 추가" && git push
```

증거 전체 색인과 통과 기준 연결은 [`docs/evidence/README.md`](docs/evidence/README.md).
